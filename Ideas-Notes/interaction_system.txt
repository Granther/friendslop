# Interaction system
- Singleton autoload
- Acts as node as child of main
- Only for client, although, probably should hook into multiplayer

# Manages
- What client is holding (direct reference)
	- Does this make sense? 
	- Pick up box, set box as item in hand, set blend for animation/hand state
	- Fix state machine
		- not allow picking up
		- expect drop
		- handle interact via some kind of method on main object

# Can only interact if the spring arm is touching it
# The interact radius only shows as close as the springarm is 

# Should interact distance be specific per item
	- Yes, because, some items may want to be more "visible"
	- ... such a terminal from ball etc
	- But, should everything be interactable from the same distance?
		- Probably, frustrating if not
	- Player has collisionshape
	- Interactables are all on same layer
	- On body entered, show label, vice versa exit
	- 
# Singlton?
# API to interact with player items?

# Maybe separate items into things that can be picked up and stationary interactibles like NPCs
# This would make it easier to assume certain methods etc

# Player presses interact while looking at something interactible etc
# We catch in scanner and we have the object 
# We pass the item to 

# What if we pass over key inputs to the item being interacted with?
# So, when a ball is held, it begins listening for inputs that are relavent to itself, then uses an API to interact with the anim_player 
