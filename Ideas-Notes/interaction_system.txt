# Interaction system
- Singleton autoload
- Acts as node as child of main
- Only for client, although, probably should hook into multiplayer

# Manages
- What client is holding (direct reference)
	- Does this make sense? 
	- Pick up box, set box as item in hand, set blend for animation/hand state
	- Fix state machine
		- not allow picking up
		- expect drop
		- handle interact via some kind of method on main object

# Can only interact if the spring arm is touching it
# The interact radius only shows as close as the springarm is 

# Should interact distance be specific per item
	- Yes, because, some items may want to be more "visible"
	- ... such a terminal from ball etc
	- But, should everything be interactable from the same distance?
		- Probably, frustrating if not
	- Player has collisionshape
	- Interactables are all on same layer
	- On body entered, show label, vice versa exit
	- 
# Singlton?
# API to interact with player items?

# Maybe separate items into things that can be picked up and stationary interactibles like NPCs
# This would make it easier to assume certain methods etc

# Player presses interact while looking at something interactible etc
# We catch in scanner and we have the object 
# We pass the item to 

# What if we pass over key inputs to the item being interacted with?
# So, when a ball is held, it begins listening for inputs that are relavent to itself, then uses an API to interact with the anim_player 

# Each item can have its own phys process, so we can keep specific phys stuffs therte
# And each item def NEEDS some kind of api to interact with the player (velocity, pos, rot, head/cam rot/pos) 
# 	So all of these can just be top level components in the player and we just have some kind of singleton to deliver it globally
# I like the idea of giving objects a lot of control over their interaction, then it makes it easier to make very custom objects later
# Nothing is completely locked in place

# Maybe a health handler? Can everything have one?
# I like the idea of a component that handles the HUD with player health data
# But, maybe a component that allows for applying effects etc, can/should we use signals
# We implement specifically for the item
# So, on missile impact, it gets all items in the aoe and c

# I'm inmagining an AEO recieving component and and AOE dishing one
# So, a grenade goes off and it has an AEO sphere for which the effects are applied
# It gets everything in that interaction area (its assumed that everything on that collision layer ...)
# It then calls the method 

# So, we have a marker 3d, and we set the hand to that position. 

# Could have Marker3D which sits in space in front of player, globally accessible in the scene and the hand is changed to this target once the item is reparented to some marker3d on the hand. So we keep the dependence in space, but don't have to deal with making a unique animation per holding mode

# Could we attach metadata to each unique item in order to remove the need for the item to manage its own holding-type/state?

# 3 systems
# Item interaction and scanning
# Body position handling/animation
# Health and state 

# Base interactable type
# Grabbable and Ridable extends

# Say we are in a vehicle and also holding an item, how do we want to handle picking up and dropping items while also leaving/getting in the vehicle?
# So, leaving the vehicle would be different than dropping an item
# Maybe a state machine to interpret inputs all in one place
# 

# Every item should be throwable, but not all throwable items are shootable

# I am imagining a central place where input comes in, then signals or some kind of API is used to distribue
# This makes it so input is very controlled
# And, then we can use a state machine to process just the API
# So we can say, move this body (ie, the veh or player) depending on the state
# We can also modify how the input is recieved


# Ok, lets talk through this. So, the grendade as has an interact coimponent and and a grab component
The interact is the only interface between the player interaction system and the item. Does that make good sense
I think so, we are then dealing with only one item, kind of like a class
Am I not completely exploring composition by doing this? I dont think there is really a better way in this scenario

# Humans are terrible at predicting the future... so, if you fuck it up just be ready to fix it later lol

# We want to pass intput from the player to other things
But do we actually want to pass it so much as just change the function that checks for it
