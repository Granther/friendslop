# Ok, so when player 1 connects, we are hosting the server and are fine

# but, when the seond player connects, we create a client

- So. player 1 opens the server and creates the player 

What do we want the arch to look like?
Player 1 creates server and listens for connecting players. When they connect we instance a player for them to 'inhabit'

Say we are player2, we connect and add_player runs, we then create a new player and add them as a child of World. When this happens we 

We also get thrown into the current game scene which contains 

Well, so, when player2 hits a ball. It keeps hitting until the server moves it
What if we had periodic tick, like once per second that fixed this 

So the main peer, 1, runs an update func on every 30 frames

When we interact with 


Everytime a collision happens, we inform the server of the col and the force, dir ect
We then do the coll on each client 

Dont forget about an interaction lock, in which we cannot play any kind of pick up animation unless we get confirmation from the server...? Maybe could scale with the chances that another player could interact at nearly the same time, maybe based off proximity

Ok, so we should be syncing per tick, but how do we know that every player is on the same tick?
We could do ticks after interaction?

Player 2 kicks ball and starts recordiing a sliding window of 60 ticks around the balls position etc

So, if all players sync the the server's phystick count when they join, not only will all players be just slightly off
depending on latencty, but also, if the servr drops, skips, or takes longer, the count gets thrown off

So, we need to sync frequently
I 

Ok, so, the missile. 
We launch the missile and apply a constant force to the transform.basis.y, which is the unit vector that goes through the vehicle

I think its cause rotation and direction are inherently different, and I havent understood that. 
We need to apply a force opposite direction of the vector which represents the difference from some Y vector

# So, all rigidbodies need to have a PhysSyncComp

# 2 different modes
# Events that cause physics thing, but are never synced
# 

# How can all items easily become extensibly multiplayer?
# Component, but how do we tie unique methods to this?
# We sync every n ticks, but we need immediate "TCP" reliable events one-time
# 

# 
